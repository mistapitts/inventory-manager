# Environment Configuration Example
# Copy this file to .env and fill in your values

# Node Environment
NODE_ENV=development

# JWT Secret (REQUIRED in production)
JWT_SECRET=your-super-secret-jwt-key-here

# Database Path (defaults to ./data/inventory.db locally)
DB_PATH=./data/inventory.db

# Upload Path (defaults to ./uploads locally)
UPLOAD_PATH=./uploads

# CORS Origins (comma-separated, leave empty for permissive dev mode)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Admin Account (optional, defaults shown)
ADMIN_EMAIL=mistapitts@gmail.com
ADMIN_PASSWORD=Admin123!

# Render Production Settings (uncomment and set for production)
# NODE_ENV=production
# JWT_SECRET=your-production-jwt-secret
# DB_PATH=/opt/data/inventory.db
# UPLOAD_PATH=/opt/data/uploads
# ALLOWED_ORIGINS=https://your-app.onrender.com
# BACKUP_PATH=/opt/data/backups
